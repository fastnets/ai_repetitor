services:
  backend:
    build: ./backend
    env_file: ./backend/.env
    environment:
      DATABASE_URL: sqlite:////app/data/tutor.db
      TEXTBOOK_INDEX: /textbooks/index.json
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/data
      - ./data/textbooks:/textbooks:ro
